import Link from "next/link";
import { ArrowUpRight, Check, LockKeyhole, Users } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { getCurrentUser, getProfile } from "@/lib/auth/guards";
import { getCurrentWorkspace, getWorkspaceMembers } from "@/lib/workspaces/current";
import { ELYQORA_MODULES } from "@/lib/modules/registry";
import { getInitials } from "@/lib/utils";

export default async function HubPage() {
  const user = await getCurrentUser();
  if (!user) return null;
  const [profile, workspace] = await Promise.all([getProfile(user.id), getCurrentWorkspace(user.id)]);
  if (!workspace) return null;
  const members = await getWorkspaceMembers(workspace.id);
  const enabled = ELYQORA_MODULES.filter((module) => module.enabled);
  const comingSoon = ELYQORA_MODULES.filter((module) => !module.enabled);
  return <div className="space-y-8"><section className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="eyebrow">{workspace.workspace_type} workspace</p><h1 className="mt-2 font-display text-4xl font-semibold tracking-tight text-ink sm:text-5xl">Good to see you, {profile?.full_name?.split(" ")[0] || "there"}.</h1><p className="mt-3 max-w-2xl text-base leading-7 text-[#667878]">This is your calm starting point for the work that matters in <span className="font-semibold text-ink">{workspace.name}</span>.</p></div><Link href="/settings/workspace"><Button variant="secondary">Workspace settings <ArrowUpRight size={16} className="ml-2" /></Button></Link></section><section className="grid gap-4 md:grid-cols-3"><Card className="bg-ink text-white"><CardContent><div className="mb-8 flex items-center justify-between"><span className="grid h-10 w-10 place-items-center rounded-xl bg-mint text-ink"><Check size={18} /></span><Badge className="bg-[#284a42] text-mint">Ready</Badge></div><div className="text-sm text-[#c9ddd2]">Foundation status</div><div className="mt-1 text-3xl font-semibold">Identity connected</div><p className="mt-2 text-sm leading-6 text-[#9db9aa]">Your account and workspace access are in place.</p></CardContent></Card><Card><CardContent><div className="mb-8 flex items-center justify-between"><span className="grid h-10 w-10 place-items-center rounded-xl bg-mint text-moss"><Users size={18} /></span><span className="text-xs font-semibold uppercase tracking-[0.12em] text-[#8a9992]">People</span></div><div className="text-sm text-[#667878]">Workspace members</div><div className="mt-1 text-3xl font-semibold text-ink">{members.length}</div><p className="mt-2 text-sm leading-6 text-[#667878]">Invite collaborators when you’re ready.</p></CardContent></Card><Card><CardContent><div className="mb-8 flex items-center justify-between"><span className="grid h-10 w-10 place-items-center rounded-xl bg-sand text-moss"><LockKeyhole size={18} /></span><span className="text-xs font-semibold uppercase tracking-[0.12em] text-[#8a9992]">Privacy</span></div><div className="text-sm text-[#667878]">Data boundary</div><div className="mt-1 text-3xl font-semibold text-ink">Workspace-only</div><p className="mt-2 text-sm leading-6 text-[#667878]">Row-level security protects tenant-owned data.</p></CardContent></Card></section><section><div className="mb-4 flex items-end justify-between"><div><p className="eyebrow">Your modules</p><h2 className="mt-2 font-display text-3xl font-semibold text-ink">A connected toolkit</h2></div><span className="text-sm text-[#667878]">{enabled.length} enabled</span></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{enabled.map((module) => <Link key={module.slug} href={`/${module.slug}`} className="focus-ring"><Card className="h-full transition hover:-translate-y-0.5 hover:border-moss"><CardContent><div className="mb-5 flex items-center justify-between"><span className="grid h-10 w-10 place-items-center rounded-xl bg-mint text-lg text-moss">{module.icon}</span><ArrowUpRight size={17} className="text-[#9aa8a2]" /></div><h3 className="font-semibold text-ink">{module.name}</h3><p className="mt-2 text-sm leading-6 text-[#667878]">{module.description}</p></CardContent></Card></Link>)}</div></section><section><div className="mb-4"><p className="eyebrow">Coming into focus</p><h2 className="mt-2 font-display text-3xl font-semibold text-ink">More of Elyqora, when you need it</h2></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{comingSoon.slice(0, 8).map((module) => <div key={module.slug} className="flex items-center gap-3 rounded-2xl border border-[var(--line)] bg-white/70 p-4"><span className="grid h-9 w-9 place-items-center rounded-xl bg-sand text-moss">{module.icon}</span><div className="min-w-0"><div className="truncate text-sm font-semibold text-ink">{module.name}</div><div className="text-xs text-[#8a9992]">Planned module</div></div></div>)}</div></section><div className="flex items-center gap-2 text-xs text-[#8a9992]"><span className="grid h-6 w-6 place-items-center rounded-lg bg-mint text-moss">{getInitials(profile?.full_name)}</span>Built for small, focused teams.</div></div>;
}
